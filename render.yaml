services:
  - type: web
    name: gold-rate-prediction
    env: python
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run gold_rate_prediction.py --server.port 10000
    pythonVersion: 3.10.13
